console.log("Umm...where are we?");

const birthYear = 1998;

let myName = "G Pull";

console.log(myName, "was born", birthYear);

// Can't change const variable's values

console.warn(myName, "Is sick af");

console.log("myName is", myName.length, "characters long.");

console.log("myName includes g", myName.toUpperCase().includes("G"));

if (password.length >= 8) {
  return false;
} else if ('Contains "password"') {
  return false;
} else if ("No uppercase characters") {
  return false;
} else {
  return true;
}
